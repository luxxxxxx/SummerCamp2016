var slider = (function () {
    //定义一些属性
    var per = ...
    //定义辅助函数
    function function_name(argument) {
        // body...
    }
    return {
        //拥有的一些方法
        pre: function (argument) {
            // body...
        },
        next: function (argument) {
            // body...
        }
    }
})();
//调用
slider.pre();
slider.next();